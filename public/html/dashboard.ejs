<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GTPSID Login</title>
  <link rel="icon" href="https://s3.eu-west-1.amazonaws.com/cdn.growtopiagame.com/website/resources/assets/images/growtopia.ico" />
  
  <style>
    /* Reset & Font Dasar */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html, body {
      height: 100%;
      background: transparent;
      color: #333;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    /* HAPUS background pattern */
    /* Background pattern dihilangkan */

    /* Container utama yang memanjang ke samping - TANPA SHADOW */
    .main-container {
      display: flex;
      width: 90%;
      max-width: 950px;
      height: 520px;
      background: linear-gradient(135deg, #3c2a21 0%, #1a1208 100%);
      border-radius: 16px;
      /* Shadow dihapus */
      overflow: hidden;
      border: 1px solid #5d4037;
      position: relative;
    }

    /* HAPUS efek dekoratif tambahan */
    /* .main-container::before dihapus */

    /* Bagian kiri untuk konten visual */
    .visual-section {
      flex: 1.2;
      background: linear-gradient(135deg, #8d6e63 0%, #5d4037 50%, #3e2723 100%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 40px;
      color: #fff;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    /* HAPUS pattern dekoratif pada bagian visual */
    /* .visual-section::before dihapus */

    .visual-section h1 {
      font-size: 42px;
      margin-bottom: 10px;
      font-weight: 300;
      text-shadow: none; /* Hapus text shadow */
      position: relative;
    }

    .visual-section h2 {
      font-size: 48px;
      margin-bottom: 30px;
      font-weight: 700;
      text-shadow: none; /* Hapus text shadow */
      position: relative;
    }

    .visual-section p {
      font-size: 16px;
      line-height: 1.7;
      max-width: 320px;
      opacity: 0.9;
      position: relative;
    }

    /* Ikon dekoratif */
    .icon-container {
      width: 80px;
      height: 80px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 25px;
      position: relative;
      backdrop-filter: blur(5px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .icon-container::before {
      content: '🌿';
      font-size: 36px;
    }

    /* Bagian kanan untuk form login */
    .login-container {
      flex: 1;
      padding: 45px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      background: rgba(30, 20, 10, 0.7);
      backdrop-filter: blur(10px);
    }

    /* Judul */
    .login-title h1 {
      font-size: 32px;
      font-weight: 300;
      color: #d7ccc8;
      margin-bottom: 5px;
      text-align: center;
      text-shadow: none; /* Hapus text shadow */
    }

    .login-title h2 {
      font-size: 36px;
      font-weight: 700;
      color: #d7ccc8;
      margin-bottom: 30px;
      text-align: center;
      text-shadow: none; /* Hapus text shadow */
    }

    /* Form Styling */
    form {
      display: flex;
      flex-direction: column;
      gap: 18px;
    }

    /* Input Fields */
    input[type="text"], input[type="password"] {
      width: 100%;
      padding: 15px 18px;
      font-size: 16px;
      background: rgba(50, 35, 25, 0.8);
      color: #efebe9;
      border: 1px solid #6d4c41;
      border-radius: 10px;
      transition: all 0.3s ease;
    }

    /* Efek fokus dengan warna coklat - TANPA SHADOW */
    input:focus {
      outline: none;
      border-color: #a1887f;
      box-shadow: none; /* Hapus box shadow */
      background: rgba(60, 40, 30, 0.9);
    }

    /* Placeholder style */
    ::placeholder {
      color: #bcaaa4;
    }

    /* Tombol Umum */
    button {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 10px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      margin-top: 8px;
    }

    button:active {
      transform: scale(0.98);
    }

    /* Tombol Primer (Login) - Gradasi coklat emas - TANPA SHADOW */
    .btn-primary {
      background: linear-gradient(45deg, #d7a461, #b08c5a);
      color: #3e2723;
      /* Shadow dihapus */
    }
    .btn-primary:hover {
      background: linear-gradient(45deg, #e0b879, #c19a64);
      /* Shadow dihapus */
    }

    /* Tombol Sekunder (Register) - Outline coklat */
    .btn-secondary {
      background: transparent;
      color: #d7a461;
      border: 2px solid #d7a461;
    }
    .btn-secondary:hover {
      background: rgba(215, 164, 97, 0.1);
    }

    /* Link "Show Password" */
    .toggle-password {
      cursor: pointer;
      color: #a1887f;
      font-size: 14px;
      text-align: right;
      margin-top: -8px;
      margin-bottom: 12px;
      transition: color 0.3s;
    }
    .toggle-password:hover {
      color: #d7a461;
    }

    /* Footer kecil */
    .footer {
      text-align: center;
      margin-top: 25px;
      color: #8d6e63;
      font-size: 12px;
    }

    /* Responsif untuk layar kecil */
    @media (max-width: 768px) {
      .main-container {
        flex-direction: column;
        height: auto;
        max-width: 420px;
        margin: 20px;
      }
      
      .visual-section {
        padding: 30px 20px;
      }
      
      .visual-section h1 {
        font-size: 32px;
      }
      
      .visual-section h2 {
        font-size: 36px;
      }
      
      .login-container {
        padding: 35px 25px;
      }
      
      .login-title h1 {
        font-size: 28px;
      }
      
      .login-title h2 {
        font-size: 32px;
      }
    }

  </style>
</head>
<body>

  <div class="main-container">
    <!-- Bagian visual di sebelah kiri -->
    <div class="visual-section">
      <div class="icon-container"></div>
      <h1>Welcome to</h1>
      <h2>GTPSID</h2>
      <p>Connect with thousands of players worldwide. Join the adventure in the world of Growtopia and explore endless possibilities!</p>
    </div>
    
    <!-- Bagian form login di sebelah kanan -->
    <div class="login-container">
      <div class="login-title">
        <h1>Log Into Your</h1>
        <h2>GTPSID</h2>
      </div>
      <form method="POST" action="/player/growid/login/validate" autocomplete="off">
        <input type="text" name="_token" placeholder="Server Name *" required />
        <input type="text" name="growId" id="loginGrowId" placeholder="Your GrowID Name *" required />
        <input type="password" name="password" id="loginPassword" placeholder="Your GrowID Password *" required />
        
        <div class="toggle-password" id="togglePassword">Show Password</div>
        
        <button type="submit" name="action" value="Login" class="btn-primary">Login</button>
        <button type="submit" name="action" value="Register" class="btn-secondary">Register</button>
      </form>
      <div class="footer">© 2025 GTPSID. All rights reserved.</div>
    </div>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const growId = localStorage.getItem("growId");
      if (growId) document.getElementById("loginGrowId").value = growId;
    });

    document.getElementById("togglePassword").addEventListener("click", () => {
      const pwd = document.getElementById("loginPassword");
      const toggle = document.getElementById("togglePassword");
      if (pwd.type === "password") {
        pwd.type = "text";
        toggle.textContent = "Hide Password";
      } else {
        pwd.type = "password";
        toggle.textContent = "Show Password";
      }
    });

    document.querySelector("form").addEventListener("submit", () => {
      const growId = document.getElementById("loginGrowId").value;
      if (growId) localStorage.setItem("growId", growId);
    });
  </script>

</body>
</html>
